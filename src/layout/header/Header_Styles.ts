import { styled } from "styled-components";

const Header = styled.header`
  position: fixed;
  top: 0;
  right: 0;
  left: 0;
  z-index: 99999;
  padding: 20px 0;
  background-color: rgba(31, 31, 32, 8.9);
`
export const S = {
  Header
}